# 实验全景记录（专利 → 企业合作推荐）

> **给新接手者/新会话通读**：本文档完整记录任务、数据、实验设计、全部结果、事故教训、资产位置与下一步。
> 读完即可接续工作。进度级更新看 PROGRESS.md（跨会话维护），代码结构看 CLAUDE.md。
> 最近更新：2026-09-09（实验全部收口）

---

## 1. 项目一句话

把 **Patton**（ACL 2023，文本丰富网络预训练框架，核心架构 **GraphFormers** = BERT 层间插入图聚合层）迁移到
**专利 → 企业合作推荐**：给定一篇专利，从 **25,598 家候选企业**（企业+高校+科研院所）中推荐真实合作对象。

采用标准的 **检索 → 重排（retrieve-then-rerank）两阶段管线**：

```
专利查询 → 稠密检索(FAISS 粗筛 top-K) → GraphFormers 精排 → 排序企业列表
```

- 检索器：双塔结构（专利向量 · 企业向量点积），毫秒级扫全库
- 重排器：查询+候选一起过深度网络，贵但准，只对粗筛出的 K 个候选跑

---

## 2. 数据

### 2.1 来源与规模

| 层 | 内容 | 规模 |
|---|---|---|
| 母库 | 6 年专利 CSV（2514 万行）清洗入库 → `patents.db`（25GB） | 861.9 万专利 / 70.2 万企业 / 868 万链接 |
| 领域库 | G 段（物理/计算）→ `g_domain.db`（7GB） | 234.8 万专利 / 270.9 万链接 |
| 候选池 | 企业+高校+科研院所（删除政府/军队/医院/事业单位等 ~5200 家） | **25,598 家**（企业 22,976 + 高校 1,334 + 科研院所 1,288） |
| 查询 | 2022-2024 年、真实申请人在候选池内的专利 | 957,218 条 → train 315,266 / val 323,356 / test 318,596（**按时间切分**，无穿越） |

"合作"的代理定义：**专利的申请人（assignee）**。申请关系 = 正例；非申请关系 = 负例。

### 2.2 节点文本与邻居（专利 → Patton 字段映射）

| 节点 | 自身文本 | 5 个邻居 |
|---|---|---|
| 专利（查询侧） | 标题+摘要+权利要求 | 同 IPC 分类的 5 篇专利（技术邻居） |
| 企业（候选侧） | 名称+经营范围+简介 | 该企业的专利组合 3 条 + 已知合作方 2 家 |

### 2.3 三阶段数据格式与量

| 阶段 | 文件 | 格式 | 数量 |
|---|---|---|---|
| ① 预训练 | `pretrain/train.jsonl` + `corpus.txt` | 对比对 {q,k,qn,kn} + 全部节点文本做 MLM | 对比对 **156.6 万**（≤2023 链接）；MLM 语料 = 234.8 万专利 + 2.6 万企业文本 |
| ② 检索 | `nc/train.text.jsonl` → 实际用 `train.half.jsonl`（抽 50% 减负） | 每条 1 正 + 8 负（4 BM25 硬 + 4 随机） | 157,633 条；验证 `val.small` ≈ 1 万（抽 1/32） |
| ③ 重排 | `nc/train.rerank.32.jsonl`（采样 10 万） | 每条 1 正 + **20 负**（纯 BM25，干扰项更难） | 100,000 条；验证 `val.rerank.small` ≈ 1 万；测试 `test.rerank.10000.jsonl` **318,596 全量** |

负例难度递进是有意的：检索学粗粒度区分（4 硬 4 随机），重排学精排（20 个"像但不是"的 BM25 对手）。

### 2.4 数据资产位置

- **服务器** `/workspace/Patton/data/patent/`（nc/ 33GB + pretrain/ 18GB，tokenize 后格式）——**未回传本地**，关机 7 天清盘
- **本地** `data/patent/nc/*.text.jsonl`（文本版干净副本，可重建 tokenize 数据）+ `documents.json/txt` + `test.truth.trec`
- **本地** `data_pipeline/`：`patents.db`(25G)、`g_domain.db`(7G)、`build_cache/cache.db`(3.6G，含 queries/doc_map/q_pos/bm25_neg 等表，**BM25 补算依赖它**)

---

## 3. 实验设计

### 3.1 为什么两阶段

31.9 万查询 × 2.5 万企业 = 80 亿对，深度模型算不完 → 粗筛压缩到 top-K 再精排。

### 3.2 三阶段训练

| 阶段 | 学什么 | 配置 | 时长 | 产物 |
|---|---|---|---|---|
| ① 预训练 | MLM 学中文专利/企业语料 + 对比学习学图邻居聚合（**企业图谱知识在此沉淀**） | batch 4×累积32, max_len 256 | 30h | `ckpt/patent/pretrain/graphformer/1e-5/` |
| ② 检索 | 对比学习：把专利/企业编码进同一向量空间 | batch 1×累积128, hn 4 | 10h02m（1231 步） | `ckpt/patent/nc_retrieval/graphformer/1e-5/` |
| ③ 重排 | 在 21 候选里把正例排第一 | batch 1×累积128, hn 4 | 6h16m（781 步） | `ckpt/patent/nc_rerank/graphformer/1e-5/checkpoint-500` |

底座传递链：chinese-roberta-wwm-ext → ①预训练 → ②'检索 → ④重排。

**12G 显存硬约束（关键背景）**：每样本 36 条子图序列（查询 6 + 正例 6 + 负例 4×6），max_len 256 下 batch 2 也 OOM →
检索/重排一律 batch 1×累积 128。**副作用：梯度累积不产生 in-batch 负例**，对比始终只有 1 正+4 负 = 5 路——这是全库 recall 弱（0.43）的结构性根因之一。

### 3.3 评测设计（训练与考试严格分离）

- **检索阶段**：不看训练内小样本指标，而是**全库实考**——对 31.9 万测试查询在 25,598 家全库 FAISS 检索，算
  **recall@50/100**（真实企业在 top-K 的比例）= 管线召回保障/天花板。
  ⚠️ 训练内 eval（5 候选 P@1 0.60）与全库 recall 是两回事，不可混用。
- **重排阶段**：对每查询的「1 正 + 20 BM25 干扰」候选集排序，算 P@1 / MRR / NDCG@5 / NDCG@10。
  ⚠️ 该评测设定保证正例在候选内；真实端到端 P@1 受检索 recall 天花板（0.43）限制——端到端联合评测未做，写论文要主动说明。
- **消融**：主模型与消融模型在**同一验证集、同参数**各测一遍，差值才是纯贡献。

### 3.4 关键设计决策（答辩防问）

1. **企业侧（k 侧）不给邻居**：训练-推理同口径（推理时 documents.txt 只有企业文本）+ 防标签泄漏（见事故一）
2. **时间切分**：查询按 2022-2024 时间分 train/val/test；预训练用 ≤2023 链接——无时间穿越
3. **检索评测不带图邻居**：search 只喂节点自身文本（q 无 q_n、k 无 k_n），即全库 recall 是在"无图"条件下测的
4. BM25 只当负例来源，粗筛必须用稠密（BM25 全库 recall@100 仅 0.034，见结果表 1）

---

## 4. 全部实验结果

### 表 1：检索阶段——全库 recall（25,598 家全库，n=318,596）

| 粗检索器 | recall@20 | recall@50 | recall@100 | recall@200 |
|---|---|---|---|---|
| BM25（bigram 词法） | 0.013 | 0.022 | 0.034 | 0.051 |
| **稠密（GraphFormers ②'）** | — | **0.343** | **0.432** | — |

> 稠密比 BM25 强 12 倍；BM25 在此任务几乎失效（专利技术语言 vs 经营范围词法重叠太低）。
> 稠密 recall@100 = 0.432 是**管线端到端天花板**。

### 表 2：重排阶段——全量测试（test.rerank.10000，318,596 查询 × 21 候选，38h41m）

| | P@1 | MRR | NDCG@5 | NDCG@10 |
|---|---|---|---|---|
| **重排器（完整管线）** | **0.817** | **0.876** | **0.891** | **0.901** |

> 81.7% 的查询把真实合作企业排第一。

### 表 3：消融——去预训练（val.rerank.small 同文件同参数，各 ~1.2h）

| | P@1 | MRR | NDCG@5 | NDCG@10 | train_loss |
|---|---|---|---|---|---|
| 有专利预训练（主模型） | **0.830** | **0.886** | **0.901** | **0.910** | 0.209 |
| 去预训练（chinese-roberta 直训重排） | 0.766 | 0.838 | 0.855 | 0.870 | 1.442 |
| **预训练贡献** | **+6.4** | **+4.8** | **+4.6** | **+4.0** | — |

> ⚠️ 该消融同时去掉 ①预训练+②'检索训练两段（主模型底座是检索 ckpt），是"合计贡献"；
> 单独归因需补「预训练→重排」中间版（未做）。
> ⚠️ 表 2 与表 3 主模型数字不同是**评测集不同**（全量 test vs 1 万 val 样本），不是模型不同。

### 表 4：负结果——检索 v2（in-batch 负例尝试）

| | recall@50 | recall@100 |
|---|---|---|
| ②'（batch 1 @ 256，无 in-batch） | **0.343** | **0.432** |
| v2（batch 4 @ 128 + hn 3，有 in-batch） | 0.277 | 0.363 |
| Δ | −6.5 | −6.9 |

> 结论：**查询文本长度主导检索质量**，max_len 128 的截断损失盖过 in-batch 负例增益。
> 附带观察：v2 训练内 eval 0.643 比 ②' 的 0.597 还高，但全库 recall 更差——**训练内小样本指标 ≠ 全库 recall**。

### 阶段递进总览

| 阶段 | 核心指标 | 数值 |
|---|---|---|
| ① 预训练 | val MRR / NDCG@10 | 0.83 / 0.87 |
| ②' 检索训练 | 训练内 P@1 / MRR / NDCG@10 | 0.60 / 0.75 / 0.81 |
| ③ 检索全库 | recall@50 / @100 | 0.343 / 0.432 |
| ⑤ 重排全量 | P@1 / MRR / NDCG@10 | **0.817 / 0.876 / 0.901** |

### 论文叙事要点（三表 + 一个诚实声明）

1. 表 1：粗筛召回保障（稠密 12× BM25）——检索是瓶颈（0.43 天花板）
2. 表 2：精排精度（0.817）——核心贡献
3. 表 3：预训练增益 +6.4 P@1——创新点证据；表 4 作为训练配置分析，显实验扎实
4. 诚实声明：表 2 的评测设定保证正例在候选内，真实端到端受 0.43 限制；端到端评测列 future work

---

## 5. 事故与教训（面试/答辩素材，也是新接手者的防坑指南）

### 事故一：标签泄漏（2026-08-27 发现，已修复）——最重要的教训

- **现象**：检索训练末段 loss <5e-5，验证 P@1 却只有 0.39
- **根因**：正例 k_n 有真实邻居、负例 5 条全空 → 邻居 mask 是模型显式输入 → 读 mask 即可 100% 分类
- **数值铁证**：NDCG@10 与 NDCG@100 小数点后 16 位完全相同（理论预测值吻合）
- **修法**：k 侧全员无邻居（`fix_kn_leak.py` 原地清空；build_patent_data.py 建数据时就置空）
- **修复后**：P@1 0.39 → 0.60——**认真学语义比作弊还强**
- **教训**：正负例除标签外构造方式必须完全一致；训练喂的字段必须和推理能拿到的字段一致；loss 掉到 0 先怀疑泄漏

### 事故二：TSV 劈裂（2026-08-28，已修复）

经营范围字段混入 \t/\n（25,598 家 1,831 家带 \n）→ documents.txt 27,429 行 ≠ 25,598。修法：写 TSV 前清洗字段内制表符/换行；JSON 转义天然安全。

### 踩坑清单（详见 PROGRESS.md「踩坑记录」）

1. 注意力显存 ∝ max_len²，256 是 32 的 8 倍
2. grad_cache 只省反向不省前向
3. nohup 下 loss 块缓冲不落盘 → 脚本统一 `python -u`
4. 存档在验证之后执行，验证 OOM 会连存档一起丢 → save_steps < eval_steps
5. search 有 pkl 结果缓存（search.py:77），换模型重跑前必须删
6. 检索脚本末尾会 `rm` trec 结果和 pkl——**想留结果先复制**
7. git pull 前 chmod +x 会导致 mode 变更冲突 → 先 `git checkout --` 再 pull 再 chmod
8. Excel 打开 18 位信用代码会转科学计数法永久损坏 → 只用文本导入
9. g_domain.db 写操作不可中途杀进程（journal_mode=OFF 下曾损坏，已重建）
10. 孤儿专利误删事故（22 万个人申请专利被删，不可恢复）——删"无链接"专利必须按 assignee 名称含机构标记过滤

---

## 6. 资产清单（本地）

### 6.1 模型 ckpt（`ckpt/patent/`，均已回传验证）

| 目录 | 内容 | 用途 |
|---|---|---|
| `pretrain/graphformer/1e-5/` | ① 预训练模型 | ②' 底座 |
| `nc_retrieval/graphformer/1e-5/` | ②' 检索模型（最终） | ③ 检索 + ④ 重排底座 |
| `nc_retrieval_leaked/` | 泄漏版（作废存档） | 对照组 |
| `nc_rerank/graphformer/1e-5/` | ④ 重排（checkpoint-500 + 最终） | **主模型**（⑤ 测试用 checkpoint-500） |
| `nc_rerank_nopretrain/` | ⑦ 消融模型 | 去预训练对照 |
| `chinese-roberta-wwm-ext/` | 中文底座（hfl 110M） | 起点 |

### 6.2 日志（`logs/patent/`，关键指标行）

| 日志 | 关键内容 |
|---|---|
| `rerank_test.log` | ⑤ 全量评测：P@1 0.817 / MRR 0.876 / NDCG@10 0.901 |
| `rerank_ablation_test.log` | ⑦ 对比：0.830（主）vs 0.766（消融） |
| `rerank.log` / `rerank_ablation.log` | ④/⑦ 训练：781 步，train_loss 0.209 / 1.442 |
| `retrieve.log` / `retrieve2.log` | ②' 训练（末次 eval 0.597）/ ③ recall（0.3426/0.4320） |
| `retrieve_v2_train.log` / `retrieve_v2.log` | ⑧ v2 训练（eval 0.643）/ v2 recall（0.2774/0.3634） |

### 6.3 关键脚本

| 脚本 | 作用 |
|---|---|
| `src/run_pretrain_patent.sh` | ① 预训练 |
| `src/nc_retrieve_train_patent.sh` / `_infer_` / `_retrieval_` | ②/③ 检索训练/建索引/全库评测 |
| `src/nc_retrieve_*_patent_v2.sh` | ⑧ 检索 v2（负结果，勿再跑） |
| `src/nc_rerank_train_patent.sh` / `_test_patent.sh` | ④/⑤ 重排训练/全量评测（`$STEP` 改最佳步） |
| `src/nc_rerank_ablation_*_patent.sh` | ⑦ 消融训练/对比测试 |
| `data_pipeline/build_patent_data.py` | 数据五阶段构建（documents/queries/neighbors/bm25/assemble） |
| `data_pipeline/fix_kn_leak.py` | 标签泄漏修复工具 |
| `data_pipeline/bm25_recall.py` | BM25 全库 recall 本地补算（scipy 稀疏，与训练负例同口径） |
| `src/scripts/eval_trec.py` | recall@K 计算（替代 trec_eval） |

### 6.4 git 关键提交（分支 patent）

| commit | 内容 |
|---|---|
| `943c9d0` | ⑥ 回传补齐，实验收口 |
| `0c1b5bd` | ⑧ 检索 v2 负结果 |
| `870de5c` | v2 OOM 修复（hn 4→3） |
| `4886bfb` | ⑧ v2 脚本 |
| `b8c6257` | ③ recall 补算 + 两阶段叙事要点 |
| `7bd7c85` | ⑦ 消融脚本 |

---

## 7. 当前状态与下一步

### 7.1 状态（2026-09-09）

- ✅ 实验全部收口：①预训练 ②'检索 ③recall ④重排训练 ⑤全量评测 ⑦消融 ⑧负结果
- ✅ 全部 ckpt + 日志回传本地并核验
- 服务器：收尾阶段待确认关机（按量计费，关机 7 天清盘——已全部回传，无损失风险）

### 7.2 下一步：写论文

- 核心表：表 1（检索）→ 表 2（重排）→ 表 3（消融）→ 表 4（负结果）
- 创新点叙事：专利域预训练承载企业图谱知识（+6.4 P@1）+ 图增强重排
- 诚实声明：端到端天花板 0.43、表 2 评测设定保证正例在候选内

### 7.3 可选补充实验（若重开服务器，按性价比排序）

| 实验 | 回答的问题 | 成本 |
|---|---|---|
| 去预训练检索（chinese-roberta 直训检索） | 预训练对检索阶段的贡献（与表 3 对称，补全归因矩阵） | ~13h |
| 「预训练→重排」中间版 | 把表 3 的 +6.4 拆成"预训练"与"检索训练"各自贡献 | ~8h+1.2h |
| 去图消融（neighbor_mask_ratio=1） | 图聚合层的直接贡献（当前所有结果都没有单独证明图的用处） | 训练+评测 |
| 端到端评测（FAISS top-100 直接喂重排） | 真实管线 P@1 | 建数据+评测 |
| case study / 错误分析 | 定性展示 | 本地/服务器 |

### 7.4 已知弱点清单（论文里要主动交代或列 future work）

1. 检索是瓶颈：全库 recall@100 0.432 = 端到端天花板
2. 检索训练无 in-batch 负例（12G 单卡硬约束）；⑧ 已证明"降长度换 batch"此路不通
3. 表 3 消融是"预训练+检索训练"合计贡献，未单独归因
4. 无图的直接消融证据缺失
5. 重排评测与真实管线候选来源（FAISS top-K）未衔接
