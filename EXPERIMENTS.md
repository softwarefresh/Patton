# 实验全景记录（专利 → 企业合作推荐）

> 实验已全部收口（2026-09-13）。本文档保留核心内容：任务与数据、实验设计与结果、关键决策与教训。
> 进度状态/操作细节/踩坑记录见 PROGRESS.md；代码结构见 CLAUDE.md。

---

## 1. 任务

把 **Patton**（ACL 2023，GraphFormers = BERT 层间插入图聚合层）迁移到**专利 → 企业合作推荐**：
给定一篇专利，从 **25,598 家候选企业**（企业+高校+科研院所）中推荐真实合作对象。
"合作"的代理定义 = 专利的**申请人**（申请关系 = 正例，其余 = 负例）。

```
专利查询 → 稠密检索(FAISS 粗筛 top-K) → GraphFormers 精排 → 排序企业列表
```

- 检索器：双塔（专利向量 · 企业向量点积），毫秒级扫全库
- 重排器：查询+候选一起过深度网络，贵但准，只跑粗筛出的 K 个候选

## 2. 数据

### 2.1 规模（层层筛选）

| 层 | 规模 |
|---|---|
| 母库 patents.db | 861.9 万专利 / 70.2 万企业 / 868 万链接 |
| G 段（物理/计算）g_domain.db | 234.8 万专利 / 270.9 万链接 |
| 候选池（企业+高校+科研院所） | **25,598 家** |
| 查询（2022-2024，申请人在池内） | 957,218 条 → train 315,266 / val 323,356 / test 318,596（**按时间切分**，无穿越） |

### 2.2 节点文本与邻居

| 节点 | 自身文本 | 5 个邻居 |
|---|---|---|
| 专利（查询侧） | 标题+摘要+权利要求 | 同 IPC 分类的 5 篇专利（技术邻居） |
| 企业（候选侧） | 名称+经营范围+简介 | 专利组合 3 条 + 合作方 2 家（**训练/推理均置空**，见 §3.3） |

### 2.3 三阶段数据

| 阶段 | 数据 | 数量 |
|---|---|---|
| ① 预训练 | 对比对 {q,k,qn,kn} + 全部节点文本（MLM） | 对比对 **156.6 万**（≤2023 链接）；MLM = 234.8 万专利 + 2.6 万企业文本 |
| ② 检索 | 每条 1 正 + 8 负（4 BM25 硬 + 4 随机）；实际训练抽 50% | 157,633 条（`train.half.jsonl`）；验证 ~1 万 |
| ③ 重排 | 每条 1 正 + **20 负**（纯 BM25，干扰项更难）；训练采样 10 万 | 100,000 条；验证 ~1 万；**测试 318,596 全量** |

负例难度递进是有意的：检索学粗粒度区分，重排学"像但不是"的对手。

## 3. 实验设计

### 3.1 三阶段训练

| 阶段 | 学什么 | 配置 | 时长 | 产物 |
|---|---|---|---|---|
| ① 预训练 | MLM 学语料 + 对比学习学图邻居聚合（企业图谱知识在此沉淀） | batch 4×累积32, max_len 256 | 30h | `ckpt/patent/pretrain/graphformer/1e-5/` |
| ② 检索 | 对比学习：专利/企业编码进同一向量空间 | batch 1×累积128, hn 4 | 10h（1231 步） | `ckpt/patent/nc_retrieval/graphformer/1e-5/` |
| ③ 重排 | 在 21 候选里把正例排第一 | batch 1×累积128, hn 4 | 6.3h（781 步） | `ckpt/patent/nc_rerank/.../checkpoint-500` |

底座链：chinese-roberta-wwm-ext → ① → ②' → ④。

**12G 显存约束**：每样本 36 条子图序列，batch 2 也 OOM → 检索/重排一律 batch 1×累积 128。
**副作用：无 in-batch 负例**（对比始终 5 路）——全库 recall 弱（0.43）的结构性根因之一。

### 3.2 评测设计

- **检索阶段**：全库实考——31.9 万测试查询在 25,598 家全库 FAISS 检索，算 **recall@50/100** = 管线召回保障/天花板。
  训练内 eval（5 候选 P@1 0.60）与全库 recall 是两回事，不可混用。
- **重排阶段**：「1 正 + 20 BM25 干扰」候选集排序，算 P@1/MRR/NDCG@5/10。
  该设定保证正例在候选内；真实端到端受检索天花板限制（端到端评测未做）。
- **消融**：主模型与消融模型**同一验证集、同参数**各测一遍，差值才是纯贡献。

### 3.3 关键设计决策（答辩防问）

1. **图只增强查询侧**：专利查询在检索/重排的训练、推理、评测**全程带 5 个同 IPC 邻居**（`test.node.text.jsonl` 的
   `n_text` 非空，`inference_dataset.py` 会编码建 mask）；企业侧全程无邻居——防泄漏（见 §5）+ 对齐 documents.txt 推理口径。
2. **时间切分**：查询按 2022-2024 时间分 train/val/test；预训练用 ≤2023 链接——无时间穿越。
3. **BM25 只当负例来源**，粗筛必须用稠密（BM25 全库 recall@100 仅 0.034）。

## 4. 实验结果（核心）

### 表 1：检索阶段——全库 recall（25,598 家全库，n=318,596）

| 粗检索器 | recall@20 | recall@50 | recall@100 | recall@200 |
|---|---|---|---|---|
| BM25（bigram 词法） | 0.013 | 0.022 | 0.034 | 0.051 |
| **稠密（GraphFormers ②'）** | — | **0.343** | **0.432** | — |

> 稠密 12 倍于 BM25（专利技术语言 vs 经营范围词法重叠太低）。稠密 recall@100 = 0.432 是**端到端天花板**。

### 表 2：重排阶段——全量测试（318,596 查询 × 21 候选，38h41m）

| | P@1 | MRR | NDCG@5 | NDCG@10 |
|---|---|---|---|---|
| **重排器（完整管线）** | **0.817** | **0.876** | **0.891** | **0.901** |

> 81.7% 的查询把真实合作企业排第一。

### 表 3：消融——去预训练（val.rerank.small 同文件同参数）

| | P@1 | MRR | NDCG@5 | NDCG@10 | train_loss |
|---|---|---|---|---|---|
| 有专利预训练（主模型） | **0.830** | **0.886** | **0.901** | **0.910** | 0.209 |
| 去预训练（原始中文 BERT 直训） | 0.766 | 0.838 | 0.855 | 0.870 | 1.442 |
| **预训练贡献** | **+6.4** | **+4.8** | **+4.6** | **+4.0** | — |

> ⚠️ 消融同时去掉 ①预训练+②'检索训练两段（主模型底座是检索 ckpt），是"合计贡献"；单独归因需补中间版。
> ⚠️ 表 2 与表 3 主模型数字不同 = **评测集不同**（全量 test vs 1 万 val 样本），不是模型不同。

### 表 4：负结果——检索 v2（in-batch 负例尝试）

| | recall@50 | recall@100 |
|---|---|---|
| ②'（batch 1 @ 256，无 in-batch） | **0.343** | **0.432** |
| v2（batch 4 @ 128 + hn 3，有 in-batch） | 0.277 | 0.363 |
| Δ | −6.5 | −6.9 |

> 结论：**查询文本长度主导检索质量**。附带观察：v2 训练内 eval 0.643 比 ②' 的 0.597 高、全库 recall 却更差——
> **训练内小样本指标 ≠ 全库 recall**。

### 阶段递进总览

| 阶段 | 核心指标 | 数值 |
|---|---|---|
| ① 预训练 | val MRR / NDCG@10 | 0.83 / 0.87 |
| ②' 检索训练 | 训练内 P@1 / MRR / NDCG@10 | 0.60 / 0.75 / 0.81 |
| ③ 检索全库 | recall@50 / @100 | 0.343 / 0.432 |
| ⑤ 重排全量 | P@1 / MRR / NDCG@10 | **0.817 / 0.876 / 0.901** |

### 论文叙事要点

1. 表 1：粗筛召回保障（稠密 12× BM25）——检索是瓶颈（0.43 天花板）
2. 表 2：精排精度 0.817——核心贡献；表 3：预训练增益 +6.4 P@1——创新点证据
3. 表 4 当训练配置分析写，显实验扎实
4. **诚实声明**：表 2 评测设定保证正例在候选内，真实端到端受 0.43 限制

## 5. 关键教训

### 事故一：标签泄漏（2026-08-27，已修复）——最重要的教训

- **现象**：检索训练末段 loss <5e-5，验证 P@1 却只有 0.39
- **根因**：正例 k_n 有邻居、负例 5 条全空 → 邻居 mask 是显式输入 → 读 mask 即 100% 分类
- **铁证**：NDCG@10 与 NDCG@100 小数点后 16 位完全相同
- **修法**：k 侧全员无邻居；修复后 P@1 0.39 → **0.60**
- **教训**：正负例除标签外构造必须一致；训练喂的字段必须和推理能拿到的字段一致；loss 掉 0 先怀疑泄漏

### 事故二：TSV 劈裂（已修复）

经营范围混入 \t/\n（25,598 家 1,831 家带 \n）→ documents.txt 行数劈裂。写 TSV 前必须清洗字段；JSON 转义天然安全。

> 其余踩坑（OOM 史、pkl 缓存、chmod 冲突、nohup 缓冲等）见 PROGRESS.md「踩坑记录」。

## 6. 资产位置（本地）

**模型**（`ckpt/patent/`，均已回传验证）：`pretrain/`（①）、`nc_retrieval/`（②'）、`nc_retrieval_leaked/`（作废对照）、
`nc_rerank/`（④ 主模型，checkpoint-500）、`nc_rerank_nopretrain/`（⑦ 消融）。

**日志**（`logs/patent/`）：`rerank_test.log`（表 2）、`rerank_ablation_test.log`（表 3）、`retrieve2.log`（表 1 稠密）、
`retrieve_v2.log`（表 4）、`retrieve.log`/`rerank.log`/`rerank_ablation.log`/`retrieve_v2_train.log`（各训练记录）。

**数据**（`data_pipeline/`）：`patents.db`(25G)、`g_domain.db`(7G)、`build_cache/cache.db`(3.6G，BM25 补算依赖)。

**关键脚本**：`src/*_patent.sh`（各阶段训练/评测）；`data_pipeline/build_patent_data.py`（数据构建）、
`fix_kn_leak.py`（泄漏修复）、`bm25_recall.py`（BM25 recall 本地补算）、`src/scripts/eval_trec.py`（recall 计算）。

## 7. 现状与下一步

**状态**：实验全部收口（①预训练 ②'检索 ③recall ④重排训练 ⑤全量评测 ⑦消融 ⑧负结果），ckpt+日志全部回传核验，服务器待关机。

**下一步：写论文**（三表结构见 §4 叙事要点）。

### 可选补充实验（若重开服务器，按性价比排序）

| 实验 | 回答的问题 | 成本 |
|---|---|---|
| 去预训练检索（chinese-roberta 直训检索） | 预训练对检索阶段的贡献（与表 3 对称，补全归因矩阵） | ~13h |
| 「预训练→重排」中间版 | 把表 3 的 +6.4 拆成"预训练"/"检索训练"各自贡献 | ~9h |
| 去图消融（查询侧 n_text 置空或 neighbor_mask_ratio=1） | 5 个技术邻居的直接贡献（当前唯一没单独验证的核心组件） | 训练+评测 |
| 端到端评测（FAISS top-100 直接喂重排） | 真实管线 P@1 | 建数据+评测 |

### 已知弱点（论文主动交代或列 future work）

1. 检索是瓶颈：全库 recall@100 0.432 = 端到端天花板
2. 检索训练无 in-batch 负例（12G 单卡硬约束）；⑧ 已证明"降长度换 batch"此路不通
3. 表 3 消融是"预训练+检索训练"合计贡献，未单独归因
4. 图的直接消融证据缺失（查询邻居、图聚合层的增益未单独测过）
5. 重排评测候选来源（人工 21 候选）与真实管线（FAISS top-K）未衔接
